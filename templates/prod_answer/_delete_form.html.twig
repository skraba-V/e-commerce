<form method="post" action="{{ path('app_prod_answer_delete', {'id': prod_answer.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ prod_answer.id) }}">
    <button class="btn">Delete</button>
</form>
